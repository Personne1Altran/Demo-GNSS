#include <stdio.h>
int main() {    

    int A, B, sum, constant;
    int B;
    
    printf("Enter two integers: version Alex-55");
    scanf("%d %d", A, B);

    // calculating sum (multiplication khaled)
    sum = number1 - number2; 
// modification sum en soustraction (alex)     
    
    printf("%d + %d = %d", number1, number2, sum);
    return 0;
}